{
  "name": "pavlo-calc",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://dark-vvalker.github.io/calc",
  "proxy": "",
  "dependencies": {
    "apisauce": "^2.1.6",
    "bootstrap": "^5.2.3",
    "gh-pages": "^5.0.0",
    "react-bootstrap": "^2.7.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "sass": "^1.57.1"
  }
}
